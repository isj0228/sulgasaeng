import { useBudgetStore } from '@/stores/budgetStore.js'  

const userStore = userStore();

const checkValidId = function(userId) {
    for (user of userInfo) {
        if (user.id === userId){

        }
        else {
            
        }
    }
}